# Database (only used inside Docker network, not exposed)
DB_PASSWORD=your_secure_password_here

# JWT
JWT_SECRET=your_random_32_plus_bytes_secret_here
JWT_EXPIRE_HOURS=24

# Web port (the only port exposed to host)
WEB_PORT=8880
